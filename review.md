
## 🔴 重大（セキュリティ上、早急に対応すべき）

**1. ほぼ全てのAPIエンドポイントに認可（認証必須化）が付いていない**
`RequireAuthorization()`が付与されているのは `/`, `/api/users/me`, `/api/users/change-password` のみです。習慣のCRUD（`/api/habits`系）、実行記録、グループ作成/参加、いいね、コメントなど、業務ロジックの中核となるAPIは**未ログインの第三者でも直接呼び出せます**。`app.UseAuthentication()/UseAuthorization()`を呼んでいても、Minimal APIではエンドポイント単位で`RequireAuthorization()`しない限り匿名アクセス可能というASP.NET Coreの仕様を誤解している可能性があります。

**2. `userId`をクライアントが自己申告する設計（IDOR）**
フロントエンドJS（`currentUserId`）がリクエストボディ/クエリで`userId`を送り、サーバー側はそれをそのまま信用してDB操作しています（例: `/api/habits` POST、`/api/users/{id}` PUT、`/api/habits/{id}/execute`、`/api/logs/{logId}/reactions`など）。ログイン中のユーザーIDは本来 `context.User` のClaimsから取得すべきで、現状は**ブラウザの開発者ツールで`userId`を書き換えるだけで他人の名前を変更、他人の習慣を削除、他人の代わりに実行記録を投稿**できてしまいます。1と組み合わさると被害はさらに大きくなります。

**3. パスワードが平文でDB保存・平文比較されている**
`Users.Password`は平文格納で、`existingUser.Password == password`という単純な文字列比較でログイン検証しています（ハッシュ化なし、ソルトなし）。DBファイルが漏洩すれば全ユーザーのパスワードがそのまま漏れます。最低でもBCrypt/Argon2等でハッシュ化すべきです。

**4. ログイン画面が実質「誰でもアカウント作成」になっている**
`/login`はユーザー名が存在しなければ**自動で新規登録してそのままログインさせる**実装です。ログインとサインアップが区別されておらず、他人が使いそうなユーザー名を先取り登録される、あるいは第三者が既存ユーザー名を知っていれば新規登録扱いにならず総当たりでパスワードを試せる、といったリスクがあります。加えてログイン試行のレート制限・アカウントロックもありません。

**5. デフォルト管理者アカウントの認証情報がUIにそのまま露出**
DB初期化時に `admin / secret-password` が自動投入され、さらにログイン画面の`placeholder`に`admin` / `secret-password`とそのまま表示されています。デモ用途だとしても、実運用に流用されると即座に踏み台にされます。

**6. 絵文字リアクション（`ReactionType`）が自由入力かつ属性コンテキストにそのまま埋め込まれXSSの余地がある**
`escapeHtml`は`& < > "`のみエスケープしシングルクォートを処理していません。一方`toggleReaction`の`onclick`属性は`'...'`で値を囲っています（例: `onclick="toggleReaction(${item.logId}, '${escapeHtml(r.emoji)}')"`）。サーバー側`/api/logs/{logId}/reactions`は`ReactionType`が空でなければ何でも受理するため、`'`を含む文字列を送ればonclick属性から脱出してスクリプトを注入できる可能性があります（保存型XSS）。

## 🟠 重要（設計・保守性）

- **単一ファイルへの集約**：ルーティング、HTML/CSS/JS（フロント全体）、DTO、エンティティが全部`Program.cs`に同居し2,000行超。Plan.mdでは「Razorコンポーネント分離」を意図していたようですが、実装ではRazorすら使わず生文字列HTMLをC#の`$$"""..."""`で返す形に変わっています。責務分離・テスト容易性が大きく損なわれています。
- **所有権チェックの欠如**：`PUT/DELETE /api/habits/{id}`はリクエストの`id`が誰の習慣かを一切確認せず更新・削除します（1・2の問題と表裏一体ですが、認可を直しても「グループメンバーの習慣を消せてしまう」といった業務ロジック上の検証が別途必要です）。
- **未実装のUIが残っている**：タイムラインの「表示フィルタ」ドロップダウンは選択肢が「すべての投稿」のみで、グループ別フィルタ機能（Plan.md 2.5）は未実装のまま画面に出ています。
- **`/health/db`が無認証でスキーマ情報を返す**：デバッグ用と思われますが、テーブル一覧を外部に公開する必要はないため本番では無効化/認証必須にすべきです。
- **マイグレーションの例外を握りつぶす**：`ALTER TABLE ... ADD COLUMN`の`try/catch`で全ての例外を無視しており、カラム重複以外の予期しないDBエラーも隠蔽してしまいます（`catch (SqliteException ex) when (...)`等で絞るべき）。

## 🟡 軽微

- `userId ?? 1`のようにデフォルト値でユーザーID 1（admin）にフォールバックする箇所が複数あり、パラメータ省略時に意図せず他人（admin）のデータを操作/参照する経路になり得ます。
- Cookie認証で`Secure`/`SameSite`属性の明示設定が見当たらず、HTTPS強制も未設定です（Cloud Run配下でも念のため`options.Cookie.SecurePolicy`等を明示することを推奨）。
- POST/PUT/DELETE系にCSRF対策（アンチフォージェリトークン等）が明示されていません。JSON fetchのため素朴なフォームCSRFのリスクは下がりますが、Cookie認証を使う以上は意識した設計が望ましいです。

## 良い点

- SQL発行は一貫してDapperのパラメータ化クエリ（`@Name`等）を使っており、SQLインジェクションの心配がある箇所は見当たりませんでした。
- タイムライン・習慣一覧などの表示テキストは`escapeHtml`を通しており、単純な`<script>`注入は概ね防げています（ただし上記6の属性コンテキストは例外）。
- 習慣削除時に関連するExecutionLogs/Likes/Commentsをトランザクション内でカスケード削除するなど、データ整合性への配慮は見られます。

---

総括すると、**最優先で直すべきは1〜3（認可漏れ・IDOR・平文パスワード）**です。これらは組み合わさることで「誰でも他人になりすまして全データを読み書き削除できる」状態になっており、公開環境にデプロイする前に必ず修正が必要です。よろしければ、これらの修正パッチ（Claimsからのユーザー特定への置き換え、パスワードハッシュ化、`RequireAuthorization()`の一括付与など）を実際にコードで作成することもできます。
